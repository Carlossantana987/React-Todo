import React from "react";

const TodoForm = () => {
  return (
    <div>
      <form>
        <input placeholder="todo..." />
        <button>Add</button>
        <button>Clear</button>
      </form>
    </div>
  );
};

export default TodoForm;
